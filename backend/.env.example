# Sunucu portu
PORT=4000

# JWT imzalama anahtari (uretimde mutlaka degistirin)
JWT_SECRET=movie-api-super-secret-key-degistir-beni
JWT_EXPIRES_IN=7d

# CORS icin izin verilen origin'ler (virgulle ayirin, * = hepsi)
CORS_ORIGIN=*

# Sayfalama varsayilanlari
DEFAULT_PAGE_SIZE=12
MAX_PAGE_SIZE=100
